[package]
name = "raft-event-loop"
version = "0.1.0"
edition = "2024"

[dependencies]
raftcore = { path = "../raftcore"}
serde = { version = "1.0.228", features = ["derive"] }
tokio = { version = "1.52.1", features = ["time", "sync", "rt", "macros"] }
