[package]
name = "raft-test-utils"
version = "0.1.0"
edition = "2024"

[dependencies]
postcard = { version = "1.1.3", features = ["alloc"]}
raft-event-loop = { path = "../raft-event-loop"}
raftcore = { path = "../raftcore" }
tokio = {version = "1", features = ["sync"]}

[dev-dependencies]
tokio = { version = "1", features =  ["rt", "macros"]}
